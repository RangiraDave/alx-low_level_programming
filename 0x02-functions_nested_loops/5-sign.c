#include "main.h"
/**
 * print_sign - check the code.
 *
 * @n: Variable
 *
 * Return: Always 0.
 */

int print_sign(int n)
{
	if (n > 0)
	{
		return ('+');
		return (1);
	}
	else if (n == 0)
	{
		return (0);
		return (0);

	}
	else
	{
		return ('-');
		return (1);
	}
}
