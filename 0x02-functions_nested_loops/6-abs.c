#include "main.h"
/**
 * _abs - computes absolute value
 * @n: The only variable
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
	int m;

	if (n > = 0)
		m = n;
	else
		m = n * (-1);
	return (m);
}
