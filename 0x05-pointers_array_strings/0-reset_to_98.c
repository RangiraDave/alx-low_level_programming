#include "main.h"
/**
 * reset_to_98 - to change the value of a variable
 * @n: A pointer to the variable
 * Return: Always 0 (Success)
 */
void reset_to_98(int *n)
{
	int t;

	*n = t;
	return (0);
}
