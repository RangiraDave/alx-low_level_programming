#include "main.h"
/**
 * _strlen - Displays the length of a string
 * @s: The pointer to use
 * Return: Always 0 (Success)
 */
int _strlen(char *s)
{
	int i;
	char A[];

	*s = A[];
	while (*s[i] != '\0')
	{
		++i;
	}
	return (i);
}
