#include "main.h"
/**
 * _strlen - Displays the length of a string
 * @s: The pointer to use
 * Return: Always 0 (Success)
 */
int _strlen(char *s)
{
	int i;
	string A;

	*s = A;
	while (*s != 0)
	{
		++i;
	}
	return (i);
}
