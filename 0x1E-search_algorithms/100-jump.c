#include "search_algos.h"

/**
 * jump_search - Function to implement jump search algorithm.
 * @array: The array to conduct the search in.
 * @size: Size of the array.
 * @value: The element to look for in the array.
 * Return: The element's index if found, and -1 when size is
 * equal to zero or the element couln't be found.
 */

int jump_search(int *array, size_t size, int value)
{
}
